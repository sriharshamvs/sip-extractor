# Preprocessing + OCR (CPU, runs anywhere)
pymupdf
opencv-python
scikit-image
numpy
pillow
paddleocr==3.5.0
paddlepaddle==3.0.0  # newer CPU builds hit a PIR/oneDNN inference bug under paddleocr 3.5.0
langchain-text-splitters  # paddlex (paddleocr dep) imports this unconditionally

# Symbol detection (GPU recommended, runs on Colab). Stage 9+.
transformers
torch
faiss-cpu

# Notebooks
ipykernel
ipython
